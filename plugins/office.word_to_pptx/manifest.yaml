id: office.word_to_pptx
name: "Office: Word → PPTX"
version: "0.1.0"
summary: "Convert a DOCX into a PPTX (stub for tests)."

runtime: dsl
entrypoint: flow.yaml

permissions:
  fs_read: true
  fs_write: true

inputs:
  type: object
  properties:
    docx_url:
      type: string
      description: "Absolute path or file:// URL to a .docx file."
  required: ["docx_url"]

outputs:
  pptx_path:
    type: string
    description: "Artifact path to the generated PPTX."
