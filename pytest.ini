# pytest.ini
[pytest]
pythonpath = 
    src
testpaths = tests
markers =
    integration: end-to-end tests requiring full stack and PDF export
filterwarnings =
    ignore:Please use `import python_multipart` instead.:PendingDeprecationWarning
